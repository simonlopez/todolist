#Server's configuration
server:
  host: 0.0.0.0
  port: 1337

#Mongoose's configurations
mongodb:
  host: localhost
  base: taskManagerDev2

#Mail configuration
mail:
  host: localhost
  port: 25
  from: admin@taskmanager.com

app:
  name: TaskManager
